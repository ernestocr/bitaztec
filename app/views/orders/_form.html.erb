<%= simple_form_for(order, 
                    defaults: { wrapper_html: { class: 'field' } },
                    html: { class: 'ui form' }) do |f| %>

  <%= f.hidden_field :amount, id: 'other' %>                  
  <%= f.association :payment_method %>

  <%= f.submit 'Comprar!', class: 'ui blue button' %>
<% end %>
