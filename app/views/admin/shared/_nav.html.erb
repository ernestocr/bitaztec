<div class="ui fixed menu">
  <div class="ui container">
    <%= link_to 'BitAztec [Admin]', root_path, class: 'header item' %>
    <%= render 'admin/shared/nav_links' %>
  
    <div class="right menu">
      <% if user_signed_in? %>
        <%= link_to 'Salir', destroy_user_session_path, method: :delete, class: 'item' %>
      <% else %>
        <%= link_to 'Entrar', new_user_session_path, class: 'item' %>
      <% end %>
    </div>
  </div>
</div>

<div class="spacer" style="width: 100%; height: 60px;"></div>
