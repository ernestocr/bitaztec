<%= simple_form_for([:admin, payment_method],
                    defaults: { wrapper_html: { class: 'field' } }, 
                    html: { class: 'ui form' }) do |f| %>
                    
  <%= f.input :name, autofocus: true %>
  <%= f.input :method, collection: ['Depósito', 'Transferencia'] %>
  <%= f.input :instructions %>
  <%= f.input :details %>
  <%= f.input :schedule %>
  <%= f.input :expires %>
  <%= f.submit 'Save', class: 'ui button' %>

<% end %>
